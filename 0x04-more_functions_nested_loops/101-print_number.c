#include "main.h"

/**
 * print_number - prints an integer with _putchar
 * @n: integer to print
 * Return: void
 */
void print_number(int n)
{
	int a, b, c;

	if (n < 0)
	{
		_putchar('-');
		a *= -1; b *= -1; c *= -1
	}
	if (a != 0)
    _putchar('0' + a)
     (b != 0)
    _putchar('0' + b)
     (c != 0)
    _putchar('0' + c)
}
