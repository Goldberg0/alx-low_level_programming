#include "main.h"

/**
 * print_number - prints an integer with _putchar
 * @n: integer to print
 * Return: void
 */
void print_number(int n)
{
	int i;

	if (i < 0)
	{
		i *= -1, i++
		_putchar('%d', i *= -1);
	}
	else
	{
		_putchar('%d', i)
	}
		
	_putchar("\n")
}
